{
  "name": "invoiceflow-landing-page",
  "version": "1.0.0",
  "description": "Professional landing page for InvoiceFlow - Simple invoicing for small businesses",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "build": "npm run build:css",
    "build:css": "postcss css/styles.css -o dist/css/styles.min.css && shx mkdir -p dist && shx cp index.html dist/ && shx cp -r css/styles.css dist/css/",
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint:html": "htmlhint *.html",
    "lint": "npm run lint:css && npm run lint:html",
    "start": "npx serve dist",
    "test:css": "stylelint \"css/**/*.css\" && echo 'CSS validation passed'",
    "deploy:preview": "npm run build && npx serve dist"
  },
  "keywords": [
    "invoicing",
    "small-business",
    "landing-page",
    "static-site"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.4.16",
    "cssnano": "^6.0.1",
    "htmlhint": "^1.1.4",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "shx": "^0.3.4",
    "stylelint": "^16.1.0",
    "stylelint-config-standard": "^36.0.0"
  }
}